services:
  emojibot:
    build: .
    env_file:
      - .env
    environment:
      STATS_FILE: /data/stats.json
    restart: unless-stopped
    volumes:
      - emojibot_data:/data

volumes:
  emojibot_data:
